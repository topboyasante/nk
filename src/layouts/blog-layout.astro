---
import BaseLayout from "./base-layout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout title={frontmatter.title}>
  <h4>{frontmatter.title}</h4>
  <p class="text-neutral-500">{frontmatter.pubDate}</p>
  <br>
  <slot />
</BaseLayout>
